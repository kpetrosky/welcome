// src/components/Header.js

import React from "react";
import "./Header.css"; // Import the CSS file for styling

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">I am excited to be here!</h1>
    </header>
  );
};

export default Header;